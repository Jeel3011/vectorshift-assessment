# Frontend environment variables — copy to .env.local for local dev,
# or set as CI/CD environment variables for production builds.
# Variables must be prefixed with REACT_APP_ to be embedded by CRA.

# URL of the backend API (no trailing slash)
REACT_APP_API_URL=http://localhost:8001
